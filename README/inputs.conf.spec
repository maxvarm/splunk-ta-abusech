[abusech_urlhausurl://<name>]
note_about_collect_interval = Collect interval should be more than 5 minutes to avoid IP block but less than 3 days due to API limitations

[abusech_threatfox://<name>]
note_about_collect_interval = Collect interval should be more than 5 minutes to avoid IP block but less than 3 days due to API limitations

[abusech_urlhauspayload://<name>]
note_about_collect_interval = Collect interval should be more than 5 minutes to avoid IP block but less than 3 days due to API limitations

[abusech_malwarebazaar://<name>]
note_about_collect_interval = Collect interval should be more than 5 minutes to avoid IP block and less than 60 minutes due to MalwareBazaar API limitations